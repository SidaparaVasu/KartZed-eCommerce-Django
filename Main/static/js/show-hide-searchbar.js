function showSearchbar() {
    var searchBarBox = document.getElementById("search-bar-box");
    searchBarBox.classList.add("toggle-searchbar");
}

function hideSearchbar() {
    var searchBarBox = document.getElementById("search-bar-box");
    searchBarBox.classList.remove("toggle-searchbar");
}